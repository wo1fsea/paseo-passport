diff --git a/packages/app/src/components/browser-pane.web.tsx b/packages/app/src/components/browser-pane.web.tsx
index f87cdb45..f2546573 100644
--- a/packages/app/src/components/browser-pane.web.tsx
+++ b/packages/app/src/components/browser-pane.web.tsx
@@ -1,6 +1,7 @@
-import { useMemo } from "react";
+import { createElement, useMemo, type CSSProperties } from "react";
 import { Text, View } from "react-native";
 import { StyleSheet, useUnistyles } from "react-native-unistyles";
+import { useBrowserStore } from "@/stores/browser-store";
 
 interface BrowserPaneProps {
   browserId: string;
@@ -13,6 +14,9 @@ interface BrowserPaneProps {
 
 export function BrowserPane({ browserId }: BrowserPaneProps) {
   const { theme } = useUnistyles();
+  const browser = useBrowserStore((state) => state.browsersById[browserId] ?? null);
+  const url = browser?.url ?? null;
+  const dispatchDashboardUrl = getDispatchDashboardUrl(url);
   const titleStyle = useMemo(
     () => [styles.title, { color: theme.colors.foreground }],
     [theme.colors.foreground],
@@ -22,8 +26,20 @@ export function BrowserPane({ browserId }: BrowserPaneProps) {
     [theme.colors.foregroundMuted],
   );
 
+  if (dispatchDashboardUrl) {
+    return (
+      <View style={styles.frameContainer}>
+        {createElement("iframe", {
+          src: dispatchDashboardUrl,
+          title: browser?.title || "Dispatch Dashboard",
+          style: iframeStyle,
+        })}
+      </View>
+    );
+  }
+
   return (
-    <View style={styles.container}>
+    <View style={styles.unsupportedContainer}>
       <Text style={titleStyle}>Browser is desktop-only</Text>
       <Text style={subtitleStyle}>
         Open this workspace in Electron to use the built-in browser.
@@ -33,8 +49,31 @@ export function BrowserPane({ browserId }: BrowserPaneProps) {
   );
 }
 
+function getDispatchDashboardUrl(value: string | null): string | null {
+  if (!value) {
+    return null;
+  }
+  if (value.startsWith("/dispatch-dashboard/")) {
+    return value;
+  }
+  if (value.startsWith("https:///dispatch-dashboard/")) {
+    return value.slice("https://".length);
+  }
+  return null;
+}
+
+const iframeStyle: CSSProperties = {
+  border: "0",
+  height: "100%",
+  width: "100%",
+};
+
 const styles = StyleSheet.create(() => ({
-  container: {
+  frameContainer: {
+    flex: 1,
+    minHeight: 0,
+  },
+  unsupportedContainer: {
     flex: 1,
     alignItems: "center",
     justifyContent: "center",
diff --git a/packages/app/src/components/split-container.tsx b/packages/app/src/components/split-container.tsx
index 68ce2992..9ae809d8 100644
--- a/packages/app/src/components/split-container.tsx
+++ b/packages/app/src/components/split-container.tsx
@@ -92,6 +92,8 @@ interface SplitContainerProps {
   onCreateDraftTab: (input: { paneId?: string }) => void;
   onCreateTerminalTab: (input: { paneId?: string }) => void;
   onCreateBrowserTab: (input: { paneId?: string }) => void;
+  dispatchDashboardAction?: { label: string; url: string } | null;
+  onOpenDispatchDashboard?: (input: { paneId?: string; url: string }) => void;
   showCreateBrowserTab?: boolean;
   buildPaneContentModel: (input: {
     paneId: string;
@@ -348,6 +350,8 @@ export function SplitContainer({
   onCreateDraftTab,
   onCreateTerminalTab,
   onCreateBrowserTab,
+  dispatchDashboardAction,
+  onOpenDispatchDashboard,
   showCreateBrowserTab,
   buildPaneContentModel,
   onFocusPane,
@@ -568,6 +572,8 @@ export function SplitContainer({
         onCreateDraftTab={onCreateDraftTab}
         onCreateTerminalTab={onCreateTerminalTab}
         onCreateBrowserTab={onCreateBrowserTab}
+        dispatchDashboardAction={dispatchDashboardAction}
+        onOpenDispatchDashboard={onOpenDispatchDashboard}
         showCreateBrowserTab={showCreateBrowserTab}
         buildPaneContentModel={buildPaneContentModel}
         onFocusPane={onFocusPane}
@@ -707,6 +713,8 @@ function SplitNodeView({
   onCreateDraftTab,
   onCreateTerminalTab,
   onCreateBrowserTab,
+  dispatchDashboardAction,
+  onOpenDispatchDashboard,
   showCreateBrowserTab,
   buildPaneContentModel,
   onFocusPane,
@@ -759,6 +767,8 @@ function SplitNodeView({
         onCreateDraftTab={onCreateDraftTab}
         onCreateTerminalTab={onCreateTerminalTab}
         onCreateBrowserTab={onCreateBrowserTab}
+        dispatchDashboardAction={dispatchDashboardAction}
+        onOpenDispatchDashboard={onOpenDispatchDashboard}
         showCreateBrowserTab={showCreateBrowserTab}
         buildPaneContentModel={buildPaneContentModel}
         onFocusPane={onFocusPane}
@@ -804,6 +814,8 @@ function SplitNodeView({
               onCreateDraftTab={onCreateDraftTab}
               onCreateTerminalTab={onCreateTerminalTab}
               onCreateBrowserTab={onCreateBrowserTab}
+              dispatchDashboardAction={dispatchDashboardAction}
+              onOpenDispatchDashboard={onOpenDispatchDashboard}
               showCreateBrowserTab={showCreateBrowserTab}
               buildPaneContentModel={buildPaneContentModel}
               onFocusPane={onFocusPane}
@@ -855,6 +867,8 @@ function SplitPaneView({
   onCreateDraftTab,
   onCreateTerminalTab,
   onCreateBrowserTab,
+  dispatchDashboardAction,
+  onOpenDispatchDashboard,
   showCreateBrowserTab,
   buildPaneContentModel,
   onFocusPane,
@@ -998,6 +1012,8 @@ function SplitPaneView({
           onCreateDraftTab={onCreateDraftTab}
           onCreateTerminalTab={onCreateTerminalTab}
           onCreateBrowserTab={onCreateBrowserTab}
+          dispatchDashboardAction={dispatchDashboardAction}
+          onOpenDispatchDashboard={onOpenDispatchDashboard}
           showCreateBrowserTab={showCreateBrowserTab}
           onReorderTabs={handleReorderTabs}
           onSplitRight={handleSplitRight}
diff --git a/packages/app/src/screens/workspace/workspace-desktop-tabs-row.tsx b/packages/app/src/screens/workspace/workspace-desktop-tabs-row.tsx
index 5b75de7a..e0595d7f 100644
--- a/packages/app/src/screens/workspace/workspace-desktop-tabs-row.tsx
+++ b/packages/app/src/screens/workspace/workspace-desktop-tabs-row.tsx
@@ -22,6 +22,7 @@ import {
   ArrowRightToLine,
   Columns2,
   Copy,
+  Gauge,
   RotateCw,
   Rows2,
   Globe,
@@ -74,6 +75,7 @@ const ThemedCopyX = withUnistyles(CopyX);
 const ThemedSquarePen = withUnistyles(SquarePen);
 const ThemedSquareTerminal = withUnistyles(SquareTerminal);
 const ThemedGlobe = withUnistyles(Globe);
+const ThemedGauge = withUnistyles(Gauge);
 const ThemedColumns2 = withUnistyles(Columns2);
 const ThemedRows2 = withUnistyles(Rows2);
 
@@ -156,6 +158,8 @@ interface WorkspaceDesktopTabsRowProps {
   onCreateDraftTab: (input: { paneId?: string }) => void;
   onCreateTerminalTab: (input: { paneId?: string }) => void;
   onCreateBrowserTab: (input: { paneId?: string }) => void;
+  dispatchDashboardAction?: { label: string; url: string } | null;
+  onOpenDispatchDashboard?: (input: { paneId?: string; url: string }) => void;
   showCreateBrowserTab?: boolean;
   disableCreateTerminal?: boolean;
   isWaitingOnTerminalReadiness?: boolean;
@@ -473,6 +477,8 @@ export function WorkspaceDesktopTabsRow({
   onCreateDraftTab,
   onCreateTerminalTab,
   onCreateBrowserTab,
+  dispatchDashboardAction = null,
+  onOpenDispatchDashboard,
   showCreateBrowserTab = false,
   disableCreateTerminal = false,
   isWaitingOnTerminalReadiness = false,
@@ -559,6 +565,13 @@ export function WorkspaceDesktopTabsRow({
     onCreateBrowserTab({ paneId });
   }, [onCreateBrowserTab, paneId]);
 
+  const handleOpenDispatchDashboard = useCallback(() => {
+    if (!dispatchDashboardAction) {
+      return;
+    }
+    onOpenDispatchDashboard?.({ paneId, url: dispatchDashboardAction.url });
+  }, [dispatchDashboardAction, onOpenDispatchDashboard, paneId]);
+
   const terminalDisabled = disableCreateTerminal || isWaitingOnTerminalReadiness;
   const newTerminalActionButtonStyle = useCallback(
     ({ hovered, pressed }: PressableStateCallbackType) => [
@@ -735,6 +748,24 @@ export function WorkspaceDesktopTabsRow({
             </TooltipContent>
           </Tooltip>
         ) : null}
+        {dispatchDashboardAction ? (
+          <Tooltip delayDuration={0} enabledOnDesktop enabledOnMobile={false}>
+            <TooltipTrigger
+              testID="workspace-open-dispatch-dashboard"
+              onPress={handleOpenDispatchDashboard}
+              accessibilityRole="button"
+              accessibilityLabel="Open Dispatch Dashboard"
+              style={newTabActionButtonStyle}
+            >
+              <ThemedGauge size={14} uniProps={mutedColorMapping} />
+            </TooltipTrigger>
+            <TooltipContent side="bottom" align="center" offset={8}>
+              <View style={styles.newTabTooltipRow}>
+                <Text style={styles.newTabTooltipText}>{dispatchDashboardAction.label}</Text>
+              </View>
+            </TooltipContent>
+          </Tooltip>
+        ) : null}
         {showPaneSplitActions ? (
           <>
             <Tooltip delayDuration={0} enabledOnDesktop enabledOnMobile={false}>
diff --git a/packages/app/src/screens/workspace/workspace-screen.tsx b/packages/app/src/screens/workspace/workspace-screen.tsx
index 38df733b..043a0534 100644
--- a/packages/app/src/screens/workspace/workspace-screen.tsx
+++ b/packages/app/src/screens/workspace/workspace-screen.tsx
@@ -106,6 +106,7 @@ import {
   resolveWorkspaceRouteId,
   type WorkspaceExecutionAuthorityResult,
 } from "@/utils/workspace-execution";
+import { fetchPassportDispatchDashboardAvailability } from "@/runtime/passport-dispatch-dashboard";
 import {
   WorkspaceTabPresentationResolver,
   WorkspaceTabIcon,
@@ -156,6 +157,7 @@ import { useContainerWidthBelow } from "@/hooks/use-container-width";
 import { buildHostRootRoute, buildSettingsHostRoute } from "@/utils/host-routes";
 
 const TERMINALS_QUERY_STALE_TIME = 5_000;
+const DISPATCH_DASHBOARD_QUERY_STALE_TIME = 5_000;
 const WORKSPACE_SETUP_AUTO_OPEN_WINDOW_MS = 30_000;
 const EMPTY_UI_TABS: WorkspaceTab[] = [];
 const EMPTY_PINNED_AGENT_IDS = new Set<string>();
@@ -2165,7 +2167,7 @@ function WorkspaceScreenContent({
 
   const handleOpenUrlInBrowserTab = useCallback(
     (url: string) => {
-      if (!persistenceKey || !getIsElectron()) {
+      if (!persistenceKey || (!getIsElectron() && !isWeb)) {
         return;
       }
       const { browserId } = createWorkspaceBrowser({ initialUrl: url });
@@ -3078,7 +3080,33 @@ function WorkspaceScreenContent({
     [createTerminalMutation.isPending, pendingTerminalCreateInput],
   );
   const showCreateBrowserTab = getIsElectron();
+  const dispatchDashboardQuery = useQuery({
+    queryKey: ["passport-dispatch-dashboard", workspaceDirectory] as const,
+    enabled: Boolean(isRouteFocused && !isMobile && (showCreateBrowserTab || isWeb) && workspaceDirectory),
+    queryFn: () => fetchPassportDispatchDashboardAvailability({ cwd: workspaceDirectory }),
+    staleTime: DISPATCH_DASHBOARD_QUERY_STALE_TIME,
+    refetchInterval: DISPATCH_DASHBOARD_QUERY_STALE_TIME,
+  });
+  const dispatchDashboardAction = useMemo(() => {
+    const availability = dispatchDashboardQuery.data;
+    if (!availability?.available) {
+      return null;
+    }
+    return {
+      label: availability.label,
+      url: availability.url,
+    };
+  }, [dispatchDashboardQuery.data]);
   const focusedPaneIdOrUndefined = useMemo(() => focusedPaneId ?? undefined, [focusedPaneId]);
+  const handleOpenDispatchDashboard = useCallback(
+    (input: { paneId?: string; url: string }) => {
+      if (input.paneId && persistenceKey) {
+        focusWorkspacePane(persistenceKey, input.paneId);
+      }
+      handleOpenUrlInBrowserTab(input.url);
+    },
+    [focusWorkspacePane, handleOpenUrlInBrowserTab, persistenceKey],
+  );
   const desktopFocusModeEnabled = useMemo(
     () => isFocusModeEnabled && !isMobile,
     [isFocusModeEnabled, isMobile],
@@ -3111,6 +3139,8 @@ function WorkspaceScreenContent({
         onCreateDraftTab={handleCreateDraftTab}
         onCreateTerminalTab={handleCreateTerminal}
         onCreateBrowserTab={handleCreateBrowserTab}
+        dispatchDashboardAction={dispatchDashboardAction}
+        onOpenDispatchDashboard={handleOpenDispatchDashboard}
         showCreateBrowserTab={showCreateBrowserTab}
         buildPaneContentModel={buildDesktopPaneContentModel}
         onFocusPane={handleFocusPane}
@@ -3145,6 +3175,8 @@ function WorkspaceScreenContent({
     handleCreateDraftTab,
     handleCreateTerminal,
     handleCreateBrowserTab,
+    dispatchDashboardAction,
+    handleOpenDispatchDashboard,
     showCreateBrowserTab,
     buildDesktopPaneContentModel,
     handleFocusPane,
@@ -3245,6 +3277,8 @@ function WorkspaceScreenContent({
                   onCreateDraftTab={handleCreateDraftTab}
                   onCreateTerminalTab={handleCreateTerminal}
                   onCreateBrowserTab={handleCreateBrowserTab}
+                  dispatchDashboardAction={dispatchDashboardAction}
+                  onOpenDispatchDashboard={handleOpenDispatchDashboard}
                   showCreateBrowserTab={showCreateBrowserTab}
                   disableCreateTerminal={createTerminalMutation.isPending}
                   isWaitingOnTerminalReadiness={pendingTerminalCreateInput !== null}
diff --git a/packages/app/src/stores/browser-store.test.ts b/packages/app/src/stores/browser-store.test.ts
index d7d03965..41b573ea 100644
--- a/packages/app/src/stores/browser-store.test.ts
+++ b/packages/app/src/stores/browser-store.test.ts
@@ -23,6 +23,12 @@ describe("browser store", () => {
     expect(normalizeWorkspaceBrowserUrl("//example.com/path")).toBe("https://example.com/path");
   });
 
+  it("keeps same-origin dispatch dashboard paths unchanged", () => {
+    expect(normalizeWorkspaceBrowserUrl("/dispatch-dashboard/20260506T090158046825Z/")).toBe(
+      "/dispatch-dashboard/20260506T090158046825Z/",
+    );
+  });
+
   it("keeps explicit protocols unchanged", () => {
     expect(normalizeWorkspaceBrowserUrl("http://localhost:8081")).toBe("http://localhost:8081");
     expect(normalizeWorkspaceBrowserUrl("https://localhost:8081")).toBe("https://localhost:8081");
diff --git a/packages/app/src/stores/browser-store.ts b/packages/app/src/stores/browser-store.ts
index e18d359c..3f1362ac 100644
--- a/packages/app/src/stores/browser-store.ts
+++ b/packages/app/src/stores/browser-store.ts
@@ -36,6 +36,9 @@ function normalizeBrowserUrl(value: string | null | undefined): string {
   if (!trimmed) {
     return "https://example.com";
   }
+  if (trimmed.startsWith("/dispatch-dashboard/")) {
+    return trimmed;
+  }
   if (/^(localhost|\d{1,3}(?:\.\d{1,3}){3}|\[[\da-fA-F:.]+])(?::\d+)?(?:[/?#]|$)/.test(trimmed)) {
     return `http://${trimmed}`;
   }

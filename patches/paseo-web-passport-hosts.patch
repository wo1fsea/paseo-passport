diff --git a/packages/app/src/app/host-runtime-bootstrap.ts b/packages/app/src/app/host-runtime-bootstrap.ts
index b75cd01f..8098dc72 100644
--- a/packages/app/src/app/host-runtime-bootstrap.ts
+++ b/packages/app/src/app/host-runtime-bootstrap.ts
@@ -1,7 +1,11 @@
 import type { ActiveWorkspaceSelection } from "@/stores/navigation-active-workspace-store";
 import type { DaemonStartResult } from "@/runtime/daemon-start-service";
 import type { Href } from "expo-router";
-import { buildHostRootRoute, buildHostWorkspaceRoute } from "@/utils/host-routes";
+import {
+  buildHostOpenProjectRoute,
+  buildHostRootRoute,
+  buildHostWorkspaceRoute,
+} from "@/utils/host-routes";
 
 export interface HostRuntimeBootstrapStore {
   boot: () => void;
@@ -61,6 +65,7 @@ export const WELCOME_ROUTE: Href = "/welcome";
 export interface ResolveStartupRedirectInput {
   pathname: string;
   anyOnlineHostServerId: string | null;
+  firstRegisteredHostServerId?: string | null;
   workspaceSelection: ActiveWorkspaceSelection | null;
   isWorkspaceSelectionLoaded: boolean;
   hasGivenUpWaitingForHost: boolean;
@@ -88,6 +93,10 @@ export function resolveStartupRedirectRoute(input: ResolveStartupRedirectInput):
   }
 
   if (input.hasGivenUpWaitingForHost) {
+    if (input.firstRegisteredHostServerId) {
+      return buildHostOpenProjectRoute(input.firstRegisteredHostServerId);
+    }
+
     return WELCOME_ROUTE;
   }
 
diff --git a/packages/app/src/app/index.test.tsx b/packages/app/src/app/index.test.tsx
index c900692f..7297716a 100644
--- a/packages/app/src/app/index.test.tsx
+++ b/packages/app/src/app/index.test.tsx
@@ -18,6 +18,7 @@ const { redirectMock, state } = vi.hoisted(() => {
       storeReady: false,
     } as HostRuntimeBootstrapState,
     anyOnlineHostServerId: null as string | null,
+    hosts: [] as { serverId: string }[],
     isWorkspaceSelectionLoaded: true,
     workspaceSelection: null as ActiveWorkspaceSelection | null,
   };
@@ -45,6 +46,10 @@ vi.mock("@/desktop/daemon/desktop-daemon", () => ({
   shouldUseDesktopDaemon: () => false,
 }));
 
+vi.mock("@/runtime/host-runtime", () => ({
+  useHosts: () => state.hosts,
+}));
+
 vi.mock("@/screens/startup-splash-screen", () => ({
   StartupSplashScreen: () => React.createElement("div", { "data-testid": "startup-splash" }),
 }));
@@ -68,6 +73,7 @@ describe("Index route startup navigation", () => {
       storeReady: false,
     };
     state.anyOnlineHostServerId = null;
+    state.hosts = [];
     state.isWorkspaceSelectionLoaded = true;
     state.workspaceSelection = null;
     redirectMock.mockReset();
@@ -145,4 +151,16 @@ describe("Index route startup navigation", () => {
 
     expect(redirectMock).toHaveBeenCalledWith("/welcome");
   });
+
+  it("opens the first registered host when the give-up timer fires with offline hosts", async () => {
+    state.bootstrapState = {
+      ...state.bootstrapState,
+      hasGivenUpWaitingForHost: true,
+    };
+    state.hosts = [{ serverId: "server-offline" }];
+
+    await renderIndex();
+
+    expect(redirectMock).toHaveBeenCalledWith("/h/server-offline/open-project");
+  });
 });
diff --git a/packages/app/src/app/index.tsx b/packages/app/src/app/index.tsx
index 9e9b537d..6cbbcbe1 100644
--- a/packages/app/src/app/index.tsx
+++ b/packages/app/src/app/index.tsx
@@ -3,6 +3,7 @@ import { Redirect, usePathname } from "expo-router";
 import { StartupSplashScreen } from "@/screens/startup-splash-screen";
 import { useEarliestOnlineHostServerId, useHostRuntimeBootstrapState } from "@/app/_layout";
 import { resolveStartupRedirectRoute } from "@/app/host-runtime-bootstrap";
+import { useHosts } from "@/runtime/host-runtime";
 import {
   getLastNavigationWorkspaceRouteSelection,
   useIsLastNavigationWorkspaceRouteSelectionLoaded,
@@ -15,11 +16,13 @@ export default function Index() {
   const pathname = usePathname();
   const bootstrapState = useHostRuntimeBootstrapState();
   const anyOnlineHostServerId = useEarliestOnlineHostServerId();
+  const hosts = useHosts();
   const isWorkspaceSelectionLoaded = useIsLastNavigationWorkspaceRouteSelectionLoaded();
 
   const redirectRoute = resolveStartupRedirectRoute({
     pathname,
     anyOnlineHostServerId,
+    firstRegisteredHostServerId: hosts[0]?.serverId ?? null,
     workspaceSelection: isWorkspaceSelectionLoaded
       ? getLastNavigationWorkspaceRouteSelection()
       : null,
diff --git a/packages/app/src/runtime/host-runtime.test.ts b/packages/app/src/runtime/host-runtime.test.ts
index 2507d278..07ae97c1 100644
--- a/packages/app/src/runtime/host-runtime.test.ts
+++ b/packages/app/src/runtime/host-runtime.test.ts
@@ -1,4 +1,5 @@
-import { describe, expect, it, vi } from "vitest";
+import AsyncStorage from "@react-native-async-storage/async-storage";
+import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
 import type {
   DaemonClient,
   ConnectionState,
@@ -15,6 +16,18 @@ import {
   type HostRuntimeSnapshot,
 } from "./host-runtime";
 
+vi.mock("@react-native-async-storage/async-storage", () => ({
+  default: {
+    getItem: vi.fn(),
+    setItem: vi.fn(),
+    removeItem: vi.fn(),
+  },
+}));
+
+const mockAsyncStorage = vi.mocked(AsyncStorage);
+const REGISTRY_STORAGE_KEY = "@paseo:daemon-registry";
+const E2E_STORAGE_KEY = "@paseo:e2e";
+
 class FakeDaemonClient {
   private state: ConnectionState = { status: "idle" };
   private listeners = new Set<(status: ConnectionState) => void>();
@@ -1159,6 +1172,168 @@ describe("HostRuntimeController", () => {
 });
 
 describe("HostRuntimeStore", () => {
+  beforeEach(() => {
+    mockAsyncStorage.getItem.mockImplementation(async (key) => {
+      if (key === E2E_STORAGE_KEY) {
+        return "1";
+      }
+      return null;
+    });
+    mockAsyncStorage.setItem.mockResolvedValue(undefined);
+    mockAsyncStorage.removeItem.mockResolvedValue(undefined);
+  });
+
+  afterEach(() => {
+    vi.unstubAllGlobals();
+  });
+
+  it("treats unauthenticated Passport hosts as an empty boot registry", async () => {
+    const fetchImpl = vi.fn(async () => new Response("Unauthorized", { status: 401 }));
+    vi.stubGlobal("fetch", fetchImpl);
+    const store = new HostRuntimeStore({
+      deps: {
+        createClient: () => new FakeDaemonClient() as unknown as DaemonClient,
+        connectToDaemon: async () => {
+          throw new Error("no fallback daemon");
+        },
+        getClientId: async () => "cid_test_runtime",
+      },
+    });
+
+    store.boot();
+    await vi.waitFor(() => {
+      expect(fetchImpl).toHaveBeenCalledWith("/api/passport/hosts", {
+        credentials: "include",
+      });
+    });
+
+    expect(store.getHosts()).toEqual([]);
+    store.syncHosts([]);
+  });
+
+  it("filters invalid Passport host profiles during boot", async () => {
+    const passportHost = makeHost({
+      serverId: "srv_passport",
+      label: "Passport host",
+    });
+    vi.stubGlobal(
+      "fetch",
+      vi.fn(async () =>
+        Response.json([
+          { serverId: "srv_invalid", connections: [] },
+          { serverId: "", connections: passportHost.connections },
+          passportHost,
+        ]),
+      ),
+    );
+    const store = new HostRuntimeStore({
+      deps: {
+        createClient: () => new FakeDaemonClient() as unknown as DaemonClient,
+        connectToDaemon: async () => {
+          throw new Error("no fallback daemon");
+        },
+        getClientId: async () => "cid_test_runtime",
+      },
+    });
+
+    store.boot();
+    await vi.waitFor(() => {
+      expect(store.getHosts().map((host) => host.serverId)).toEqual(["srv_passport"]);
+    });
+
+    store.syncHosts([]);
+  });
+
+  it("merges Passport hosts by server id while preserving browser-only hosts", async () => {
+    const browserOnlyHost = makeHost({
+      serverId: "srv_browser",
+      label: "Browser only",
+    });
+    const browserCopyOfPassportHost = makeHost({
+      serverId: "srv_shared",
+      label: "Old browser label",
+      connections: [
+        {
+          id: "direct:old.example.test:6767",
+          type: "directTcp",
+          endpoint: "old.example.test:6767",
+        },
+      ],
+    });
+    const passportCopy = makeHost({
+      serverId: "srv_shared",
+      label: "Passport label",
+      connections: [
+        {
+          id: "relay:relay.paseo.sh:443",
+          type: "relay",
+          relayEndpoint: "relay.paseo.sh:443",
+          daemonPublicKeyB64: "pk_passport",
+        },
+      ],
+    });
+    mockAsyncStorage.getItem.mockImplementation(async (key) => {
+      if (key === REGISTRY_STORAGE_KEY) {
+        return JSON.stringify([browserOnlyHost, browserCopyOfPassportHost]);
+      }
+      if (key === E2E_STORAGE_KEY) {
+        return "1";
+      }
+      return null;
+    });
+    vi.stubGlobal("fetch", vi.fn(async () => Response.json([passportCopy])));
+    const store = new HostRuntimeStore({
+      deps: {
+        createClient: () => new FakeDaemonClient() as unknown as DaemonClient,
+        connectToDaemon: async () => {
+          throw new Error("no fallback daemon");
+        },
+        getClientId: async () => "cid_test_runtime",
+      },
+    });
+
+    store.boot();
+    await vi.waitFor(() => {
+      expect(store.getHosts().map((host) => host.serverId)).toEqual(["srv_browser", "srv_shared"]);
+      expect(store.getHosts().find((host) => host.serverId === "srv_shared")?.label).toBe(
+        "Passport label",
+      );
+    });
+
+    expect(store.getHosts().find((host) => host.serverId === "srv_browser")?.label).toBe(
+      "Browser only",
+    );
+    store.syncHosts([]);
+  });
+
+  it("does not write Passport-loaded hosts into browser registry storage", async () => {
+    const passportHost = makeHost({
+      serverId: "srv_passport",
+      label: "Passport host",
+    });
+    vi.stubGlobal("fetch", vi.fn(async () => Response.json([passportHost])));
+    const store = new HostRuntimeStore({
+      deps: {
+        createClient: () => new FakeDaemonClient() as unknown as DaemonClient,
+        connectToDaemon: async () => {
+          throw new Error("no fallback daemon");
+        },
+        getClientId: async () => "cid_test_runtime",
+      },
+    });
+
+    store.boot();
+    await vi.waitFor(() => {
+      expect(store.getHosts().map((host) => host.serverId)).toEqual(["srv_passport"]);
+    });
+
+    expect(mockAsyncStorage.setItem).not.toHaveBeenCalledWith(
+      REGISTRY_STORAGE_KEY,
+      expect.stringContaining("srv_passport"),
+    );
+    store.syncHosts([]);
+  });
+
   it("bootstraps agent directory subscription when host transitions online", async () => {
     const host = makeHost({
       connections: [
diff --git a/packages/app/src/runtime/host-runtime.ts b/packages/app/src/runtime/host-runtime.ts
index 351b5a1f..1b57f59e 100644
--- a/packages/app/src/runtime/host-runtime.ts
+++ b/packages/app/src/runtime/host-runtime.ts
@@ -37,6 +37,7 @@ import {
   buildLocalDaemonTransportUrl,
   createDesktopLocalDaemonTransportFactory,
 } from "@/desktop/daemon/desktop-daemon-transport";
+import { loadPassportHostProfiles } from "@/runtime/passport-hosts";
 import { replaceFetchedAgentDirectory } from "@/utils/agent-directory-sync";
 import { useSessionStore } from "@/stores/session-store";
 
@@ -179,6 +180,20 @@ function readFetchAgentsNextCursor(
   return null;
 }
 
+function mergeHostProfiles(
+  localHosts: readonly HostProfile[],
+  passportHosts: readonly HostProfile[],
+): HostProfile[] {
+  const next = new Map<string, HostProfile>();
+  for (const host of localHosts) {
+    next.set(host.serverId, host);
+  }
+  for (const host of passportHosts) {
+    next.set(host.serverId, host);
+  }
+  return Array.from(next.values());
+}
+
 function toActiveConnection(connection: HostConnection): ActiveConnection {
   if (connection.type === "directSocket") {
     return {
@@ -1240,6 +1255,7 @@ export class HostRuntimeStore {
 
   private async runBoot(): Promise<void> {
     await this.loadFromStorage();
+    await this.loadPassportHosts();
 
     const override = readConfiguredLocalDaemonOverride();
     if (override) {
@@ -1288,6 +1304,21 @@ export class HostRuntimeStore {
     }
   }
 
+  private async loadPassportHosts(): Promise<void> {
+    try {
+      const passportHosts = await loadPassportHostProfiles();
+      if (passportHosts.length === 0) {
+        return;
+      }
+      const merged = mergeHostProfiles(this.hosts, passportHosts);
+      this.hosts = merged;
+      this.syncHosts(merged);
+      this.emitHostList();
+    } catch (error) {
+      console.error("[HostRuntime] Failed to load Passport hosts", error);
+    }
+  }
+
   private async bootstrapDefaultLocalhost(): Promise<void> {
     const connection = connectionFromListen(LOCALHOST_FALLBACK_ENDPOINT);
     if (!connection || registryHasConnection(this.hosts, connection)) {
diff --git a/packages/app/src/runtime/passport-hosts.ts b/packages/app/src/runtime/passport-hosts.ts
new file mode 100644
index 00000000..05a0138a
--- /dev/null
+++ b/packages/app/src/runtime/passport-hosts.ts
@@ -0,0 +1,23 @@
+import { normalizeStoredHostProfile, type HostProfile } from "@/types/host-connection";
+
+export async function loadPassportHostProfiles(
+  fetchImpl: typeof fetch = fetch,
+): Promise<HostProfile[]> {
+  const response = await fetchImpl("/api/passport/hosts", {
+    credentials: "include",
+  });
+  if (response.status === 401) {
+    return [];
+  }
+  if (!response.ok) {
+    throw new Error("Unable to load Passport hosts.");
+  }
+
+  const parsed = (await response.json()) as unknown;
+  if (!Array.isArray(parsed)) {
+    return [];
+  }
+  return parsed
+    .map((entry) => normalizeStoredHostProfile(entry))
+    .filter((entry): entry is HostProfile => entry !== null);
+}
